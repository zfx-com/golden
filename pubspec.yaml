name: golden
description: Golden test with devices tools.
version: 0.2.0
homepage: https://github.com/zfx-com/golden

environment:
  sdk: ">=2.15.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  golden_toolkit: ^0.13.0
  meta: ^1.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  flutter_localizations:
    sdk: flutter
flutter:
